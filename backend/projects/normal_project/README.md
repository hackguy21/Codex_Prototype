# normal_project

Use this directory for manually added prototype feature code.

When vibe-coding or adding hand-written backend logic that does not yet deserve a dedicated top-level project module, place the implementation here first. Keep each feature in its own small module or subdirectory, and move it out only when it becomes a stable standalone project boundary.
